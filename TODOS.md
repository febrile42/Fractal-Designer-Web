# TODOS

## ~~Cancel tokens for in-flight renders~~ ✓ Done

## ~~WebGPU migration~~ ✓ Done

All rendering now runs in the browser via WebGPU compute shaders.
Static `index.html` — no server, no build step.
